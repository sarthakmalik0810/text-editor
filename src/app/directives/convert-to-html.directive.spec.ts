import { ConvertToHTMLDirective } from './convert-to-html.directive';

describe('ConvertToHTMLDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvertToHTMLDirective();
    expect(directive).toBeTruthy();
  });
});
